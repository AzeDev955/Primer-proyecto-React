import { test, expect, describe, vi } from "vitest";
import { render, screen } from "@testing-library/react";
import { Pokedex } from "./Pokedex";

describe("Pokedex", () => {
  test("debe llamar a onAdd al hacer click y debe sacar un pokemon");
});
